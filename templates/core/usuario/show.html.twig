{% extends 'base.html.twig' %}

{% block title %}Usuario{% endblock %}

{% block body %}
    <h1>Usuario</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ usuario.id }}</td>
            </tr>
            <tr>
                <th>Username</th>
                <td>{{ usuario.username }}</td>
            </tr>
            <tr>
                <th>Roles</th>
                <td>{{ usuario.roles ? usuario.roles|json_encode : '' }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ usuario.password }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_core_usuario_index') }}">back to list</a>

    <a href="{{ path('app_core_usuario_edit', {'id': usuario.id}) }}">edit</a>

    {{ include('core/usuario/_delete_form.html.twig') }}
{% endblock %}
